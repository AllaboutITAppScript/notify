<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🔔 ระบบแจ้งเตือนรวมศูนย์</title>
    <meta name="theme-color" content="#1a1a2e">
    <meta name="description" content="ระบบแจ้งเตือนรวมศูนย์แบบข้ามเครื่อง ทำงานได้แม้ปิดแอปและล็อคหน้าจอ">
    
    <!-- PWA Configuration -->
    <link rel="manifest" href="manifest.json">
    
    <!-- CSS & Fonts -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: #1a1a2e;
            color: #e5e7eb;
            font-family: 'Sarabun', sans-serif;
            -webkit-tap-highlight-color: transparent;
            min-height: 100vh;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Prompt', sans-serif;
        }
        
        /* Loading screen */
        #loadingScreen {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            transition: opacity 0.5s;
        }
        
        .app-icon {
            width: 80px;
            height: 80px;
            border-radius: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 32px;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
            position: relative;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        /* Notification badge */
        .notification-badge {
            background-color: #FF3B30;
            color: white;
            border-radius: 10px;
            padding: 2px 6px;
            font-size: 12px;
            font-weight: bold;
            position: absolute;
            top: -5px;
            right: -5px;
            min-width: 18px;
            height: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1;
        }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
        }
        
        ::-webkit-scrollbar-track {
            background: transparent;
        }
        
        ::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 3px;
        }
        
        /* Notification panel */
        .notification-panel {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(10px);
            z-index: 100;
            display: flex;
            flex-direction: column;
            transform: translateY(100%);
            transition: transform 0.3s ease-out;
        }
        
        .notification-panel.active {
            transform: translateY(0);
        }
        
        /* Status indicators */
        .online { color: #10b981; }
        .offline { color: #ef4444; }
        
        /* Install prompt */
        #installPrompt {
            position: fixed;
            bottom: 80px;
            left: 16px;
            right: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 12px;
            padding: 16px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            z-index: 1000;
            display: none;
        }
        
        /* Connection status bar */
        #connectionStatusBar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #10b981, #3b82f6);
            z-index: 999;
            transform: translateY(-100%);
            transition: transform 0.3s;
        }
        
        #connectionStatusBar.show {
            transform: translateY(0);
        }
        
        /* Emergency alert */
        .emergency-alert {
            position: fixed;
            top: 20px;
            left: 20px;
            right: 20px;
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            border-radius: 12px;
            padding: 16px;
            box-shadow: 0 0 30px rgba(239, 68, 68, 0.5);
            z-index: 999;
            animation: pulse 1s infinite;
            display: none;
        }
        
        /* Priority indicators */
        .priority-low { border-left-color: #3b82f6 !important; }
        .priority-medium { border-left-color: #f59e0b !important; }
        .priority-high { border-left-color: #ef4444 !important; }
        
        /* Filter buttons */
        .filter-active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
            color: white !important;
        }
        
        /* Public alarm indicator */
        .public-alarm-indicator {
            background: linear-gradient(135deg, #10b981 0%, #047857 100%);
            color: white;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 11px;
            font-weight: bold;
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }
    </style>
</head>
<body>
    <!-- Loading Screen -->
    <div id="loadingScreen">
        <div class="app-icon">
            🔔
        </div>
        <h1 class="text-2xl font-bold mb-2">ระบบแจ้งเตือนรวมศูนย์</h1>
        <p class="text-gray-400 mb-6" id="loadingText">กำลังเริ่มต้นระบบ...</p>
        <div class="w-64 h-2 bg-gray-800 rounded-full overflow-hidden">
            <div id="loadingProgress" class="h-full bg-gradient-to-r from-blue-500 to-purple-600 transition-all duration-300" style="width: 0%"></div>
        </div>
    </div>
    
    <!-- Main App -->
    <div id="mainApp" class="min-h-screen pb-24 hidden">
        <!-- Header -->
        <header class="sticky top-0 z-30 bg-gray-900/95 backdrop-blur-lg border-b border-gray-800">
            <div class="container mx-auto px-4 py-4">
                <div class="flex justify-between items-center">
                    <div class="flex items-center">
                        <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center mr-3 relative">
                            🔔
                            <div id="headerIconBadge" class="notification-badge hidden">0</div>
                        </div>
                        <div>
                            <h1 class="font-bold text-lg">ระบบแจ้งเตือน</h1>
                            <div class="flex items-center">
                                <div id="statusDot" class="w-2 h-2 rounded-full bg-green-500 mr-2"></div>
                                <span id="statusText" class="text-xs online">ออนไลน์</span>
                                <span class="text-gray-500 mx-2">•</span>
                                <span id="deviceInfo" class="text-xs text-gray-400">พีซี</span>
                            </div>
                        </div>
                    </div>
                    <div class="flex items-center space-x-3">
                        <button onclick="openNotificationPanel()" class="relative w-10 h-10 rounded-full bg-gray-800 hover:bg-gray-700 flex items-center justify-center transition-colors">
                            <i class="fas fa-bell"></i>
                            <div id="headerBadge" class="notification-badge hidden">0</div>
                        </button>
                        <button onclick="refreshAllData()" id="refreshBtn" class="w-10 h-10 rounded-full bg-gray-800 hover:bg-gray-700 flex items-center justify-center transition-colors">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                    </div>
                </div>
            </div>
        </header>
        
        <!-- Connection Status -->
        <div id="connectionStatus" class="container mx-auto px-4 py-6">
            <div class="bg-gradient-to-r from-green-900/50 to-green-800/30 rounded-xl p-4 border border-green-700/50">
                <div class="flex items-center">
                    <div class="w-3 h-3 rounded-full bg-green-500 mr-3"></div>
                    <div class="flex-1">
                        <h3 class="font-bold text-green-300" id="connectionMessage">เชื่อมต่อกับเซิร์ฟเวอร์สำเร็จ</h3>
                        <p class="text-sm text-green-400 mt-1" id="connectionDetails">ระบบพร้อมใช้งาน</p>
                    </div>
                </div>
                <div class="mt-4 grid grid-cols-4 gap-3 text-center">
                    <div>
                        <div class="text-2xl font-bold text-blue-300" id="alarmCount">0</div>
                        <div class="text-xs text-blue-400 mt-1">แจ้งเตือน</div>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-green-300" id="syncedCount">0</div>
                        <div class="text-xs text-green-400 mt-1">แจ้งทุกคน</div>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-purple-300" id="broadcastCount">0</div>
                        <div class="text-xs text-purple-400 mt-1">ประกาศ</div>
                    </div>
                    <div>
                        <div class="text-2xl font-bold text-yellow-300" id="notificationCount">0</div>
                        <div class="text-xs text-yellow-400 mt-1">การแจ้งเตือน</div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Current Time -->
        <div class="container mx-auto px-4 mb-6">
            <div class="bg-gray-800 rounded-xl p-6">
                <div class="text-center">
                    <div id="currentTime" class="text-3xl font-bold mb-2">--:--:--</div>
                    <div id="currentDate" class="text-gray-400 mb-4">-</div>
                    <div class="flex justify-between">
                        <div class="text-left">
                            <div class="text-sm text-gray-400">แจ้งเตือนถัดไป</div>
                            <div id="nextAlarm" class="font-medium">ไม่มี</div>
                        </div>
                        <div class="text-right">
                            <div class="text-sm text-gray-400">ผู้ใช้</div>
                            <div class="font-medium" id="userName">คุณ</div>
                        </div>
                    </div>
                    <div class="mt-4 pt-4 border-t border-gray-700">
                        <div class="text-sm text-gray-400">สถานะระบบ</div>
                        <div class="flex items-center justify-center mt-2">
                            <div id="serviceWorkerStatus" class="text-xs px-2 py-1 bg-green-900/30 text-green-300 rounded mr-2">
                                <i class="fas fa-cloud mr-1"></i>ทำงานเบื้องหลัง
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Quick Actions -->
        <div class="container mx-auto px-4 mb-6">
            <h2 class="text-lg font-bold mb-3">การดำเนินการด่วน</h2>
            <div class="grid grid-cols-4 gap-3">
                <button onclick="scrollToForm()" class="bg-gray-800 hover:bg-gray-700 rounded-xl p-4 flex flex-col items-center transition-colors">
                    <i class="fas fa-plus-circle text-blue-400 text-2xl mb-2"></i>
                    <span class="text-sm">เพิ่มแจ้งเตือน</span>
                </button>
                <button onclick="openNotificationPanel()" class="bg-gray-800 hover:bg-gray-700 rounded-xl p-4 flex flex-col items-center transition-colors relative">
                    <i class="fas fa-bell text-yellow-400 text-2xl mb-2"></i>
                    <span class="text-sm">ดูแจ้งเตือน</span>
                    <div id="actionBadge" class="notification-badge hidden">0</div>
                </button>
                <button onclick="scrollToBroadcast()" class="bg-gray-800 hover:bg-gray-700 rounded-xl p-4 flex flex-col items-center transition-colors">
                    <i class="fas fa-broadcast-tower text-purple-400 text-2xl mb-2"></i>
                    <span class="text-sm">ส่งประกาศ</span>
                </button>
                <button onclick="testBackgroundNotification()" class="bg-gray-800 hover:bg-gray-700 rounded-xl p-4 flex flex-col items-center transition-colors">
                    <i class="fas fa-vibrate text-green-400 text-2xl mb-2"></i>
                    <span class="text-sm">ทดสอบ</span>
                </button>
            </div>
        </div>

        <!-- Alarm Filters -->
        <div class="container mx-auto px-4 mb-4">
            <div class="bg-gray-800 rounded-xl p-4">
                <h2 class="text-lg font-bold mb-3">ตัวกรองแจ้งเตือน</h2>
                <div class="flex flex-wrap gap-2">
                    <button onclick="filterAlarms('all')" id="filterAll" class="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm transition-colors filter-active">
                        <i class="fas fa-list mr-1"></i>ทั้งหมด
                    </button>
                    <button onclick="filterAlarms('personal')" id="filterPersonal" class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors">
                        <i class="fas fa-user mr-1"></i>ส่วนตัว
                    </button>
                    <button onclick="filterAlarms('public')" id="filterPublic" class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors">
                        <i class="fas fa-users mr-1"></i>แจ้งทุกคน
                    </button>
                    <button onclick="filterAlarms('today')" id="filterToday" class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors">
                        <i class="fas fa-calendar-day mr-1"></i>วันนี้
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Add Alarm Form -->
        <div id="alarmFormSection" class="container mx-auto px-4 mb-8">
            <div class="bg-gray-800 rounded-xl p-6">
                <h2 class="text-lg font-bold mb-4">เพิ่มการแจ้งเตือนใหม่</h2>
                <form id="alarmForm" class="space-y-4">
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">หัวข้อแจ้งเตือน *</label>
                        <input type="text" id="alarmTitle" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors" placeholder="เช่น ประชุมทีม, นัดพบแพทย์" required>
                    </div>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-gray-400 text-sm mb-2">วันที่ *</label>
                            <input type="date" id="alarmDate" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors" required>
                        </div>
                        <div>
                            <label class="block text-gray-400 text-sm mb-2">เวลา *</label>
                            <input type="time" id="alarmTime" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors" required>
                        </div>
                    </div>
                    
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">รายละเอียด</label>
                        <textarea id="alarmDescription" rows="2" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors" placeholder="รายละเอียดการแจ้งเตือน"></textarea>
                    </div>
                    
                    <!-- Alarm Type Selection -->
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">ประเภทการแจ้งเตือน *</label>
                        <div class="grid grid-cols-2 gap-3">
                            <div>
                                <input type="radio" id="alarmTypePersonal" name="alarmType" value="personal" checked class="hidden peer">
                                <label for="alarmTypePersonal" class="flex flex-col items-center p-4 bg-gray-900 border-2 border-gray-700 rounded-xl cursor-pointer peer-checked:border-blue-500 peer-checked:bg-blue-900/20 transition-colors">
                                    <i class="fas fa-user text-blue-400 text-2xl mb-2"></i>
                                    <span class="font-medium">ส่วนตัว</span>
                                    <span class="text-xs text-gray-400 mt-1 text-center">แจ้งเตือนเฉพาะคุณ</span>
                                </label>
                            </div>
                            <div>
                                <input type="radio" id="alarmTypePublic" name="alarmType" value="public" class="hidden peer">
                                <label for="alarmTypePublic" class="flex flex-col items-center p-4 bg-gray-900 border-2 border-gray-700 rounded-xl cursor-pointer peer-checked:border-green-500 peer-checked:bg-green-900/20 transition-colors">
                                    <i class="fas fa-users text-green-400 text-2xl mb-2"></i>
                                    <span class="font-medium">แจ้งทุกคน</span>
                                    <span class="text-xs text-gray-400 mt-1 text-center">แจ้งเตือนทุกคนในระบบ</span>
                                </label>
                            </div>
                        </div>
                    </div>
                    
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">ระดับความสำคัญ</label>
                        <div class="flex flex-wrap gap-2">
                            <button type="button" onclick="setPriority('low')" class="px-4 py-2 bg-blue-600 rounded-lg text-sm transition-colors" id="priorityLow">ต่ำ</button>
                            <button type="button" onclick="setPriority('medium')" class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors" id="priorityMedium">กลาง</button>
                            <button type="button" onclick="setPriority('high')" class="px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors" id="priorityHigh">สูง</button>
                        </div>
                    </div>
                    
                    <div class="pt-4">
                        <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-lg font-medium transition-colors">
                            <i class="fas fa-plus-circle mr-2"></i>ตั้งเวลาแจ้งเตือน
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <!-- Active Alarms -->
        <div class="container mx-auto px-4 mb-8">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-lg font-bold">การแจ้งเตือนทั้งหมด</h2>
                <div class="flex space-x-2">
                    <button onclick="refreshAllData()" class="text-blue-400 text-sm hover:text-blue-300 px-3 py-1 bg-blue-900/20 rounded-lg transition-colors">
                        <i class="fas fa-sync-alt mr-1"></i>รีเฟรช
                    </button>
                </div>
            </div>
            <div id="alarmsList" class="space-y-3">
                <!-- Alarms will be loaded here -->
                <div class="bg-gray-800 rounded-xl p-6 text-center">
                    <i class="fas fa-clock text-3xl text-gray-500 mb-3"></i>
                    <p class="text-gray-400">ยังไม่มีกำหนดการแจ้งเตือน</p>
                    <p class="text-sm text-gray-500 mt-2">เพิ่มการแจ้งเตือนใหม่โดยใช้แบบฟอร์มด้านบน</p>
                </div>
            </div>
        </div>
        
        <!-- Broadcast Section -->
        <div id="broadcastSection" class="container mx-auto px-4 mb-20">
            <div class="bg-gradient-to-r from-purple-900/50 to-purple-800/30 rounded-xl p-6 border border-purple-700/50">
                <h2 class="text-lg font-bold mb-4 text-purple-300">
                    <i class="fas fa-broadcast-tower mr-2"></i>ส่งประกาศไปทุกเครื่อง
                </h2>
                <div class="space-y-4">
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">หัวข้อประกาศ *</label>
                        <input type="text" id="broadcastTitle" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-colors" placeholder="เช่น ประชุมด่วน, ปิดระบบชั่วคราว" required>
                    </div>
                    <div>
                        <label class="block text-gray-400 text-sm mb-2">ข้อความประกาศ *</label>
                        <textarea id="broadcastMessage" rows="2" class="w-full p-3 bg-gray-900 border border-gray-700 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-colors" placeholder="ข้อความที่ต้องการส่งไปทุกเครื่อง" required></textarea>
                    </div>
                    <div class="grid grid-cols-2 gap-3">
                        <button onclick="sendBroadcast()" class="bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-lg font-medium transition-colors">
                            <i class="fas fa-paper-plane mr-2"></i>ส่งประกาศ
                        </button>
                        <button onclick="clearBroadcast()" class="bg-gray-700 hover:bg-gray-600 text-white py-3 rounded-lg font-medium transition-colors">
                            <i class="fas fa-trash mr-2"></i>ล้าง
                        </button>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Notifications Panel -->
        <div id="notificationsPanel" class="notification-panel">
            <div class="p-4 border-b border-gray-800 bg-gray-900">
                <div class="flex justify-between items-center">
                    <h2 class="text-xl font-bold">การแจ้งเตือน</h2>
                    <div class="flex items-center space-x-2">
                        <button onclick="markAllAsRead()" class="text-blue-400 hover:text-blue-300 text-sm px-3 py-1 bg-blue-900/20 rounded-lg transition-colors">
                            <i class="fas fa-check-double mr-1"></i>อ่านทั้งหมด
                        </button>
                        <button onclick="closeNotificationPanel()" class="text-gray-400 hover:text-white">
                            <i class="fas fa-times text-xl"></i>
                        </button>
                    </div>
                </div>
            </div>
            <div id="notificationsList" class="flex-1 overflow-y-auto p-4">
                <!-- Notifications will be loaded here -->
                <div class="text-center py-10">
                    <i class="fas fa-bell-slash text-4xl text-gray-500 mb-3"></i>
                    <p class="text-gray-400">ไม่มีการแจ้งเตือน</p>
                </div>
            </div>
            <div class="p-4 border-t border-gray-800 bg-gray-900">
                <button onclick="clearAllNotifications()" class="w-full bg-red-600 hover:bg-red-700 text-white py-3 rounded-lg font-medium transition-colors">
                    <i class="fas fa-trash mr-2"></i>ล้างทั้งหมด
                </button>
            </div>
        </div>
        
        <!-- Bottom Navigation -->
        <div class="fixed bottom-0 left-0 right-0 bg-gray-900 border-t border-gray-800 z-40">
            <div class="flex justify-around p-3">
                <button onclick="scrollToTop()" class="flex flex-col items-center text-gray-400 hover:text-white transition-colors p-2">
                    <i class="fas fa-home text-xl mb-1"></i>
                    <span class="text-xs">หน้าแรก</span>
                </button>
                <button onclick="scrollToForm()" class="flex flex-col items-center text-gray-400 hover:text-white transition-colors p-2">
                    <i class="fas fa-plus text-xl mb-1"></i>
                    <span class="text-xs">เพิ่ม</span>
                </button>
                <button onclick="openNotificationPanel()" class="flex flex-col items-center text-gray-400 hover:text-white transition-colors p-2 relative">
                    <i class="fas fa-bell text-xl mb-1"></i>
                    <span class="text-xs">แจ้งเตือน</span>
                    <div id="bottomBadge" class="notification-badge hidden">0</div>
                </button>
                <button onclick="scrollToBroadcast()" class="flex flex-col items-center text-gray-400 hover:text-white transition-colors p-2">
                    <i class="fas fa-broadcast-tower text-xl mb-1"></i>
                    <span class="text-xs">ประกาศ</span>
                </button>
            </div>
        </div>
    </div>
    
    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <script>
        // ============================================
        // ตัวแปรและค่าคงที่
        // ============================================
        const GOOGLE_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbz5ML3sUKN-RCK7mWk9ABoDf6wLOQXqXK83l8Gq4lNwGLIu0aV4Zqgv2gJvxWpQdGlzD8g/exec";
        const VERSION = "2.0.0";
        
        let alarms = [];
        let notifications = [];
        let broadcasts = [];
        let userId = '';
        let deviceId = '';
        let userName = '';
        let currentPriority = 'medium';
        let checkAlarmInterval = null;
        let updateTimeInterval = null;
        let checkPublicAlarmsInterval = null;
        let syncDataInterval = null;
        let lastPublicAlarmCheck = 0;
        let serviceWorkerRegistration = null;
        let isOnline = true;
        let currentFilter = 'all';
        
        // ============================================
        // เริ่มต้นแอปพลิเคชัน
        // ============================================
        async function initApp() {
            console.log('🚀 กำลังเริ่มต้นระบบแจ้งเตือน... เวอร์ชัน', VERSION);
            
            // ตรวจสอบการเชื่อมต่อ
            setupNetworkListener();
            
            // ตรวจสอบการอนุญาต Notification
            checkNotificationPermission();
            
            // เริ่ม Service Worker สำหรับแจ้งเตือนเบื้องหลัง
            await initServiceWorker();
            updateLoadingText('กำลังตั้งค่าการแจ้งเตือนเบื้องหลัง...', 20);
            
            // โหลดข้อมูลพื้นฐาน
            updateLoadingText('กำลังเตรียมระบบ...', 10);
            
            setTimeout(() => {
                loadUserData();
                updateLoadingText('กำลังโหลดข้อมูลผู้ใช้...', 30);
            }, 500);
            
            setTimeout(() => {
                loadAppData();
                updateLoadingText('กำลังโหลดข้อมูลแจ้งเตือน...', 50);
            }, 1000);
            
            setTimeout(() => {
                setupForm();
                updateLoadingText('กำลังตั้งค่าแบบฟอร์ม...', 70);
            }, 1500);
            
            setTimeout(() => {
                startTimers();
                testConnection();
                updateLoadingText('กำลังเชื่อมต่อเซิร์ฟเวอร์...', 85);
            }, 2000);
            
            setTimeout(() => {
                finishLoading();
                
                // แสดงคำแนะนำสำหรับผู้ใช้ใหม่
                const isFirstTime = !localStorage.getItem('appInitialized');
                if (isFirstTime) {
                    setTimeout(() => showWelcomeGuide(), 1000);
                    localStorage.setItem('appInitialized', 'true');
                }
                
            }, 2500);
        }
        
        function updateLoadingText(text, progress) {
            document.getElementById('loadingText').textContent = text;
            document.getElementById('loadingProgress').style.width = progress + '%';
        }
        
        function loadUserData() {
            // สร้างหรือดึง User ID
            userId = localStorage.getItem('userId');
            if (!userId) {
                userId = 'user_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                localStorage.setItem('userId', userId);
            }
            
            // สร้างหรือดึง Device ID
            deviceId = localStorage.getItem('deviceId');
            if (!deviceId) {
                deviceId = 'device_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                localStorage.setItem('deviceId', deviceId);
            }
            
            // สร้างหรือดึง User Name
            userName = localStorage.getItem('userName');
            if (!userName) {
                const names = ['ผู้ดูแลระบบ', 'สมาชิก', 'ผู้ใช้งาน', 'เจ้าหน้าที่'];
                userName = `${names[Math.floor(Math.random() * names.length)]} ${Math.floor(Math.random() * 1000)}`;
                localStorage.setItem('userName', userName);
            }
            
            document.getElementById('userName').textContent = userName;
            
            // ตรวจสอบประเภทอุปกรณ์
            const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
            document.getElementById('deviceInfo').textContent = isMobile ? 'มือถือ' : 'พีซี';
        }
        
        function loadAppData() {
            // โหลดข้อมูลจาก localStorage
            alarms = JSON.parse(localStorage.getItem('alarms')) || [];
            notifications = JSON.parse(localStorage.getItem('notifications')) || [];
            broadcasts = JSON.parse(localStorage.getItem('broadcasts')) || [];
            
            // เรียงลำดับ alarms
            alarms.sort((a, b) => new Date(a.datetime) - new Date(b.datetime));
            
            // เรียงลำดับ notifications (ใหม่ขึ้นก่อน)
            notifications.sort((a, b) => new Date(b.time) - new Date(a.time));
            
            // อัปเดต UI
            updateAlarmsList();
            updateNotificationsList();
            updateCounts();
            updateBadges();
        }
        
        function setupForm() {
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('alarmDate').min = today;
            document.getElementById('alarmDate').value = today;
            
            const nextHour = new Date();
            nextHour.setHours(nextHour.getHours() + 1);
            document.getElementById('alarmTime').value = nextHour.getHours().toString().padStart(2, '0') + ':00';
            
            setPriority('medium');
        }
        
        function startTimers() {
            // เริ่มจับเวลาแจ้งเตือน
            if (checkAlarmInterval) clearInterval(checkAlarmInterval);
            checkAlarmInterval = setInterval(checkAlarms, 1000);
            
            // เริ่มอัปเดตเวลา
            if (updateTimeInterval) clearInterval(updateTimeInterval);
            updateTimeInterval = setInterval(updateCurrentTime, 1000);
            
            // เริ่มตรวจสอบแจ้งเตือนสาธารณะใหม่ (ทุก 30 วินาที)
            if (checkPublicAlarmsInterval) clearInterval(checkPublicAlarmsInterval);
            checkPublicAlarmsInterval = setInterval(checkForPublicAlarms, 30000);
            
            updateCurrentTime();
            
            // ตรวจสอบแจ้งเตือนสาธารณะทันทีหลังจากโหลด
            setTimeout(() => checkForPublicAlarms(), 2000);
        }
        
        function finishLoading() {
            updateLoadingText('พร้อมใช้งาน!', 100);
            
            setTimeout(() => {
                document.getElementById('loadingScreen').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loadingScreen').style.display = 'none';
                    document.getElementById('mainApp').classList.remove('hidden');
                    
                    console.log('✅ ระบบพร้อมใช้งาน');
                    
                    // ส่ง welcome notification
                    setTimeout(() => {
                        showNotification('ระบบแจ้งเตือนพร้อมใช้งาน', 'คุณสามารถตั้งเวลาและรับประกาศจากทีมงานได้ทันที', 'welcome');
                    }, 1000);
                    
                }, 300);
            }, 500);
        }
        
        // ============================================
        // Service Worker และ Background Notifications
        // ============================================
        async function initServiceWorker() {
            if ('serviceWorker' in navigator && 'PushManager' in window) {
                try {
                    // ลงทะเบียน Service Worker
                    serviceWorkerRegistration = await navigator.serviceWorker.register('sw.js');
                    console.log('✅ Service Worker ถูกติดตั้ง:', serviceWorkerRegistration);
                    
                    // ตั้งค่าการสื่อสาร
                    setupServiceWorkerCommunication();
                    
                    document.getElementById('serviceWorkerStatus').innerHTML = '<i class="fas fa-cloud mr-1"></i>ทำงานเบื้องหลัง';
                    document.getElementById('serviceWorkerStatus').className = 'text-xs px-2 py-1 bg-green-900/30 text-green-300 rounded mr-2';
                    
                    return true;
                    
                } catch (error) {
                    console.error('❌ Service Worker registration failed:', error);
                    showAlert('warning', 'ไม่สามารถตั้งค่าแจ้งเตือนเบื้องหลังได้');
                    
                    document.getElementById('serviceWorkerStatus').innerHTML = '<i class="fas fa-exclamation-triangle mr-1"></i>เบราว์เซอร์ไม่รองรับ';
                    document.getElementById('serviceWorkerStatus').className = 'text-xs px-2 py-1 bg-red-900/30 text-red-300 rounded mr-2';
                    return false;
                }
            } else {
                console.log('❌ Service Worker not supported');
                showAlert('warning', 'เบราว์เซอร์นี้ไม่รองรับการแจ้งเตือนเบื้องหลัง');
                
                document.getElementById('serviceWorkerStatus').innerHTML = '<i class="fas fa-exclamation-triangle mr-1"></i>เบราว์เซอร์ไม่รองรับ';
                document.getElementById('serviceWorkerStatus').className = 'text-xs px-2 py-1 bg-red-900/30 text-red-300 rounded mr-2';
                return false;
            }
        }
        
        function setupServiceWorkerCommunication() {
            // รับข้อความจาก Service Worker
            navigator.serviceWorker.addEventListener('message', (event) => {
                console.log('📨 ข้อความจาก Service Worker:', event.data);
                
                switch(event.data.type) {
                    case 'ALARM_TRIGGERED':
                        handleAlarmTriggered(event.data.alarm);
                        break;
                        
                    case 'NOTIFICATION_CLICKED':
                        handleNotificationClicked(event.data.data);
                        break;
                        
                    case 'NEW_PUBLIC_ALARMS':
                        console.log('📢 มีแจ้งเตือนสาธารณะใหม่:', event.data.count);
                        checkForPublicAlarms();
                        break;
                }
            });
        }
        
        // ============================================
        // ฟังก์ชันจัดการเวลา
        // ============================================
        function updateCurrentTime() {
            const now = new Date();
            
            // อัปเดตเวลา
            document.getElementById('currentTime').textContent = 
                now.toLocaleTimeString('th-TH', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
            
            document.getElementById('currentDate').textContent = 
                now.toLocaleDateString('th-TH', { 
                    weekday: 'long', 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric' 
                });
            
            // อัปเดตแจ้งเตือนถัดไป
            updateNextAlarm();
        }
        
        function updateNextAlarm() {
            const now = new Date();
            const nextAlarm = alarms.find(alarm => !alarm.triggered && new Date(alarm.datetime) > now);
            
            if (nextAlarm) {
                const alarmDate = new Date(nextAlarm.datetime);
                const timeDiff = alarmDate - now;
                const hoursDiff = Math.floor(timeDiff / (1000 * 60 * 60));
                const minutesDiff = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
                
                let timeText = '';
                if (hoursDiff > 0) {
                    timeText = `${hoursDiff} ชม. ${minutesDiff} นาที`;
                } else {
                    timeText = `${minutesDiff} นาที`;
                }
                
                document.getElementById('nextAlarm').innerHTML = `
                    <div class="text-blue-300">${nextAlarm.title}</div>
                    <div class="text-sm text-gray-400">อีก ${timeText}</div>
                `;
            } else {
                document.getElementById('nextAlarm').textContent = 'ไม่มี';
            }
        }
        
        // ============================================
        // ฟังก์ชันจัดการฟอร์ม
        // ============================================
        function setPriority(value) {
            currentPriority = value;
            
            // รีเซ็ตปุ่มทั้งหมด
            ['low', 'medium', 'high'].forEach(type => {
                const btn = document.getElementById('priority' + type.charAt(0).toUpperCase() + type.slice(1));
                if (btn) btn.className = 'px-4 py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-sm transition-colors';
            });
            
            // ตั้งค่าปุ่มที่เลือก
            const selectedBtn = document.getElementById('priority' + value.charAt(0).toUpperCase() + value.slice(1));
            if (selectedBtn) {
                if (value === 'low') {
                    selectedBtn.className = 'px-4 py-2 bg-blue-600 rounded-lg text-sm transition-colors';
                } else if (value === 'medium') {
                    selectedBtn.className = 'px-4 py-2 bg-yellow-600 rounded-lg text-sm transition-colors';
                } else if (value === 'high') {
                    selectedBtn.className = 'px-4 py-2 bg-red-600 rounded-lg text-sm transition-colors';
                }
            }
        }
        
        document.getElementById('alarmForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const title = document.getElementById('alarmTitle').value.trim();
            const date = document.getElementById('alarmDate').value;
            const time = document.getElementById('alarmTime').value;
            const description = document.getElementById('alarmDescription').value.trim();
            
            // รับค่าประเภทการแจ้งเตือน
            const alarmType = document.querySelector('input[name="alarmType"]:checked').value;
            
            if (!title || !date || !time) {
                showAlert('error', 'กรุณากรอกข้อมูลให้ครบถ้วน');
                return;
            }
            
            const datetime = new Date(`${date}T${time}`);
            
            if (datetime <= new Date()) {
                showAlert('warning', 'กรุณาตั้งเวลาในอนาคต');
                return;
            }
            
            const alarm = {
                id: 'alarm_' + Date.now() + '_' + Math.random().toString(36).substr(2, 5),
                title: title,
                description: description,
                date: date,
                time: time,
                datetime: datetime.toISOString(),
                type: alarmType,
                priority: currentPriority,
                triggered: false,
                synced: false,
                createdAt: new Date().toISOString(),
                userId: userId,
                deviceId: deviceId,
                userName: userName
            };
            
            // เพิ่มลงในรายการ
            alarms.push(alarm);
            localStorage.setItem('alarms', JSON.stringify(alarms));
            
            // บันทึกลง Google Sheets
            let saved = false;
            try {
                saved = await saveToGoogleSheets(alarm, 'created');
                alarm.synced = saved;
                localStorage.setItem('alarms', JSON.stringify(alarms));
            } catch (error) {
                console.log('⚠️ ไม่สามารถบันทึกลง Google Sheets:', error);
            }
            
            // รีเซ็ตฟอร์ม
            document.getElementById('alarmForm').reset();
            setupForm();
            
            // อัปเดต UI
            updateAlarmsList();
            updateCounts();
            
            // แสดงผลสำเร็จ
            const alarmTypeText = alarmType === 'personal' ? 'ส่วนตัว' : 'แจ้งทุกคน';
            showAlert('success', `ตั้งเวลา "${title}" สำเร็จ! (${alarmTypeText})`);
            
            // ถ้าเป็นแจ้งเตือน "แจ้งทุกคน" ให้แจ้งเตือนเครื่องอื่นทันที
            if (alarmType === 'public') {
                showNotification('📢 แจ้งเตือนสาธารณะใหม่', `"${title}" จาก ${userName}`, 'public_alarm');
            }
        });
        
        // ============================================
        // ฟังก์ชันจัดการ Google Sheets
        // ============================================
        async function testConnection() {
            try {
                const response = await fetch(GOOGLE_SCRIPT_URL + '?action=test', {
                    method: 'GET',
                    mode: 'no-cors'
                });
                
                showConnectionStatus(true, 'เชื่อมต่อกับเซิร์ฟเวอร์สำเร็จ', 'ระบบพร้อมใช้งาน');
                return true;
                
            } catch (error) {
                console.log('⚠️ ไม่สามารถทดสอบการเชื่อมต่อได้:', error);
                showConnectionStatus(false, 'ออฟไลน์', 'ทำงานเฉพาะในเครื่อง');
                return false;
            }
        }
        
        async function saveToGoogleSheets(alarm, status = 'created') {
            try {
                const payload = {
                    action: 'add_alarm',
                    date: alarm.date,
                    time: alarm.time,
                    title: alarm.title,
                    description: alarm.description || '',
                    type: alarm.type,
                    priority: alarm.priority,
                    status: status,
                    user_id: userId,
                    user_name: userName,
                    alarm_id: alarm.id,
                    device_id: deviceId,
                    timestamp: Date.now(),
                    datetime: alarm.datetime
                };
                
                const params = new URLSearchParams();
                for (const key in payload) {
                    if (payload[key] !== undefined && payload[key] !== null) {
                        params.append(key, payload[key].toString());
                    }
                }
                
                const url = `${GOOGLE_SCRIPT_URL}?${params.toString()}`;
                
                // ใช้ no-cors mode สำหรับ Google Apps Script
                const response = await fetch(url, {
                    method: 'GET',
                    mode: 'no-cors'
                });
                
                console.log('✅ บันทึกลง Sheets สำเร็จ');
                return true;
                
            } catch (error) {
                console.error('❌ ไม่สามารถบันทึกลง Google Sheets:', error);
                return false;
            }
        }
        
        // ============================================
        // ฟังก์ชันจัดการการแจ้งเตือน
        // ============================================
        function checkAlarms() {
            const now = new Date();
            
            alarms.forEach((alarm, index) => {
                if (!alarm.triggered && new Date(alarm.datetime) <= now) {
                    triggerAlarm(alarm);
                }
            });
        }
        
        async function triggerAlarm(alarm) {
            console.log('🔔 แจ้งเตือน:', alarm.title, 'ประเภท:', alarm.type);
            
            // อัปเดตสถานะ
            alarm.triggered = true;
            alarm.triggeredAt = new Date().toISOString();
            localStorage.setItem('alarms', JSON.stringify(alarms));
            
            // สำหรับแอปที่เปิดอยู่
            if (document.visibilityState === 'visible') {
                // แสดงในแอป
                const typeText = alarm.type === 'personal' ? ' (ส่วนตัว)' : ' (แจ้งทุกคน)';
                showPushNotification(alarm.title + typeText, alarm.description || 'เวลาแจ้งเตือนถึงแล้ว!', 'alarm');
            }
            
            // เพิ่มใน notifications
            const typeText = alarm.type === 'personal' ? ' (ส่วนตัว)' : ' (แจ้งทุกคน)';
            addNotification({
                id: 'alarm_' + alarm.id,
                type: 'alarm',
                title: alarm.title + typeText,
                message: alarm.description || 'เวลาแจ้งเตือนถึงแล้ว!',
                time: new Date().toISOString(),
                alarmId: alarm.id,
                read: false,
                urgent: true,
                alarmType: alarm.type
            });
            
            // ถ้าเป็นแจ้งเตือน "แจ้งทุกคน" ให้อัปเดตสถานะในเซิร์ฟเวอร์
            if (alarm.type === 'public') {
                try {
                    await saveToGoogleSheets(alarm, 'triggered');
                } catch (error) {
                    console.error('ไม่สามารถอัปเดตสถานะแจ้งเตือนสาธารณะ:', error);
                }
            }
            
            // อัปเดต UI
            updateAlarmsList();
            updateCounts();
            updateBadges();
        }
        
        // ฟังก์ชันใหม่: ตรวจสอบแจ้งเตือนสาธารณะใหม่
        async function checkForPublicAlarms() {
            if (!isOnline) return;
            
            try {
                const payload = {
                    action: 'get_public_alarms',
                    user_id: userId,
                    device_id: deviceId,
                    last_check: lastPublicAlarmCheck || 0,
                    timestamp: Date.now()
                };
                
                const params = new URLSearchParams();
                for (const key in payload) {
                    params.append(key, payload[key]);
                }
                
                const url = `${GOOGLE_SCRIPT_URL}?${params.toString()}`;
                const response = await fetch(url, { 
                    method: 'GET',
                    mode: 'no-cors'
                });
                
                // เนื่องจากใช้ no-cors เราไม่สามารถอ่าน response ได้
                // แต่เราสามารถพยายามโหลดแจ้งเตือนสาธารณะจาก localStorage แบบอื่น
                
                // อัปเดตเวลาตรวจสอบล่าสุด
                lastPublicAlarmCheck = Date.now();
                
            } catch (error) {
                console.log('⚠️ ไม่สามารถตรวจสอบแจ้งเตือนสาธารณะใหม่:', error);
            }
        }
        
        // ============================================
        // ฟังก์ชันจัดการ Broadcast (แจ้งเตือนข้ามเครื่อง)
        // ============================================
        async function sendBroadcast() {
            const title = document.getElementById('broadcastTitle').value.trim();
            const message = document.getElementById('broadcastMessage').value.trim();
            
            if (!title || !message) {
                showAlert('error', 'กรุณากรอกหัวข้อและข้อความประกาศ');
                return;
            }
            
            await sendBroadcastToServer(title, message);
        }
        
        async function sendBroadcastToServer(title, message) {
            const broadcast = {
                id: 'broadcast_' + Date.now() + '_' + Math.random().toString(36).substr(2, 5),
                type: 'broadcast',
                title: '📢 ' + title,
                message: message,
                time: new Date().toISOString(),
                sender: userId,
                senderName: userName
            };
            
            // เพิ่มใน local broadcasts
            broadcasts.unshift(broadcast);
            if (broadcasts.length > 100) {
                broadcasts = broadcasts.slice(0, 100);
            }
            localStorage.setItem('broadcasts', JSON.stringify(broadcasts));
            
            // สำหรับแอปที่เปิดอยู่
            if (document.visibilityState === 'visible') {
                showNotification(broadcast.title, broadcast.message, 'broadcast');
            }
            
            // เพิ่มใน notifications
            addNotification({
                id: broadcast.id,
                type: 'broadcast',
                title: broadcast.title,
                message: broadcast.message,
                time: broadcast.time,
                read: false
            });
            
            // พยายามบันทึกลง Google Sheets (เพื่อแจ้งเตือนเครื่องอื่น)
            try {
                await saveBroadcastToSheets(title, message);
                showAlert('success', 'ส่งประกาศสำเร็จ!');
            } catch (error) {
                console.error('ไม่สามารถบันทึกประกาศ:', error);
                showAlert('warning', 'ส่งประกาศเฉพาะเครื่องนี้สำเร็จ');
            }
            
            // ล้างฟอร์ม
            clearBroadcast();
            
            // อัปเดต UI
            updateCounts();
            updateBadges();
        }
        
        async function saveBroadcastToSheets(title, message) {
            try {
                const payload = {
                    action: 'add_broadcast',
                    title: title,
                    message: message,
                    user_id: userId,
                    user_name: userName,
                    device_id: deviceId,
                    timestamp: Date.now()
                };
                
                const params = new URLSearchParams();
                for (const key in payload) {
                    if (payload[key] !== undefined && payload[key] !== null) {
                        params.append(key, payload[key].toString());
                    }
                }
                
                const url = `${GOOGLE_SCRIPT_URL}?${params.toString()}`;
                const response = await fetch(url, { 
                    method: 'GET',
                    mode: 'no-cors'
                });
                
                console.log('✅ บันทึกประกาศสำเร็จ');
                return true;
            } catch (error) {
                console.error('❌ ไม่สามารถบันทึกประกาศ:', error);
                return false;
            }
        }
        
        function clearBroadcast() {
            document.getElementById('broadcastTitle').value = '';
            document.getElementById('broadcastMessage').value = '';
        }
        
        // ============================================
        // ฟังก์ชันแสดงการแจ้งเตือน
        // ============================================
        function showNotification(title, message, type = 'info') {
            // ตรวจสอบการอนุญาต
            if (Notification.permission !== 'granted') return;
            
            const options = {
                body: message,
                icon: '/icon.png',
                badge: '/icon.png',
                tag: type + '_' + Date.now(),
                requireInteraction: type.includes('urgent') || type.includes('alarm'),
                vibrate: [200, 100, 200],
                data: {
                    url: window.location.href,
                    type: type,
                    time: Date.now()
                }
            };
            
            const notification = new Notification(title, options);
            
            notification.onclick = function() {
                window.focus();
                notification.close();
                
                if (type.includes('broadcast')) {
                    openNotificationPanel();
                }
            };
            
            setTimeout(() => {
                notification.close();
            }, 5000);
        }
        
        function showPushNotification(title, message, type = 'info') {
            // แสดงการแจ้งเตือนในแอป
            showAlert('info', title, message);
        }
        
        // ============================================
        // ฟังก์ชันจัดการ Alarms List
        // ============================================
        function updateAlarmsList() {
            const container = document.getElementById('alarmsList');
            
            const filteredAlarms = filterAlarmsByType(currentFilter);
            
            if (filteredAlarms.length === 0) {
                let message = '';
                switch(currentFilter) {
                    case 'personal':
                        message = 'ยังไม่มีกำหนดการแจ้งเตือนส่วนตัว';
                        break;
                    case 'public':
                        message = 'ยังไม่มีกำหนดการแจ้งเตือนสาธารณะ';
                        break;
                    case 'today':
                        message = 'ไม่มีแจ้งเตือนสำหรับวันนี้';
                        break;
                    default:
                        message = 'ยังไม่มีกำหนดการแจ้งเตือน';
                }
                
                container.innerHTML = `
                    <div class="bg-gray-800 rounded-xl p-6 text-center">
                        <i class="fas fa-clock text-3xl text-gray-500 mb-3"></i>
                        <p class="text-gray-400">${message}</p>
                        <p class="text-sm text-gray-500 mt-2">เพิ่มการแจ้งเตือนใหม่โดยใช้แบบฟอร์มด้านบน</p>
                    </div>
                `;
                return;
            }
            
            filteredAlarms.sort((a, b) => new Date(a.datetime) - new Date(b.datetime));
            
            container.innerHTML = '';
            const now = new Date();
            
            filteredAlarms.forEach(alarm => {
                const alarmDate = new Date(alarm.datetime);
                const isPast = alarmDate < now;
                const isTriggered = alarm.triggered;
                
                const timeDiff = alarmDate - now;
                const hoursDiff = Math.floor(timeDiff / (1000 * 60 * 60));
                const minutesDiff = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
                
                let timeStatus = '';
                let timeClass = '';
                
                if (isTriggered) {
                    timeStatus = '<i class="fas fa-check mr-1"></i>แจ้งเตือนแล้ว';
                    timeClass = 'bg-green-900/30 text-green-300';
                } else if (isPast) {
                    timeStatus = '<i class="fas fa-clock mr-1"></i>เลยเวลา';
                    timeClass = 'bg-gray-700 text-gray-300';
                } else if (hoursDiff < 1) {
                    timeStatus = `<i class="fas fa-hourglass-end mr-1"></i>${minutesDiff} นาที`;
                    timeClass = 'bg-red-900/30 text-red-300';
                } else if (hoursDiff < 24) {
                    timeStatus = `<i class="fas fa-hourglass-half mr-1"></i>${hoursDiff} ชม.`;
                    timeClass = 'bg-yellow-900/30 text-yellow-300';
                } else {
                    const daysDiff = Math.floor(hoursDiff / 24);
                    timeStatus = `<i class="fas fa-calendar mr-1"></i>${daysDiff} วัน`;
                    timeClass = 'bg-blue-900/30 text-blue-300';
                }
                
                const typeClass = alarm.type === 'personal' ? 'alarm-type-private' : 'alarm-type-public';
                const typeIcon = alarm.type === 'personal' ? 'fa-user' : 'fa-users';
                const typeText = alarm.type === 'personal' ? 'ส่วนตัว' : 'แจ้งทุกคน';
                
                const priorityClass = `priority-${alarm.priority}`;
                const priorityText = alarm.priority === 'low' ? 'ต่ำ' : alarm.priority === 'medium' ? 'กลาง' : 'สูง';
                const priorityColor = alarm.priority === 'low' ? 'blue' : alarm.priority === 'medium' ? 'yellow' : 'red';
                
                const isExternal = alarm.userId !== userId;
                
                const alarmElement = document.createElement('div');
                alarmElement.className = `bg-gray-800 rounded-xl p-4 hover:bg-gray-750 transition-colors border-l-4 ${priorityClass}`;
                alarmElement.innerHTML = `
                    <div class="flex justify-between items-start">
                        <div class="flex-1">
                            <div class="flex items-start">
                                <div class="mr-3 ${isTriggered ? 'text-gray-500' : 'text-blue-400'}">
                                    <i class="fas fa-${isTriggered ? 'check-circle' : 'clock'} text-lg"></i>
                                </div>
                                <div class="flex-1">
                                    <div class="flex justify-between items-start">
                                        <h3 class="font-bold ${isTriggered ? 'text-gray-400' : 'text-white'}">
                                            ${alarm.title} 
                                            ${isExternal ? '<span class="public-alarm-indicator ml-2"><i class="fas fa-users mr-1"></i>จากภายนอก</span>' : ''}
                                        </h3>
                                        <div class="flex space-x-1">
                                            <span class="text-xs px-2 py-1 ${typeClass} text-white rounded">
                                                <i class="fas ${typeIcon} mr-1"></i>${typeText}
                                            </span>
                                            <span class="text-xs px-2 py-1 bg-${priorityColor}-900/30 text-${priorityColor}-300 rounded">
                                                ${priorityText}
                                            </span>
                                        </div>
                                    </div>
                                    <p class="text-gray-400 text-sm mt-1">${alarm.description || 'ไม่มีรายละเอียด'}</p>
                                    <p class="text-gray-500 text-xs mt-2">
                                        <i class="fas fa-user-circle mr-1"></i>
                                        ${alarm.userName || 'ไม่ทราบผู้ใช้'}
                                    </p>
                                    <div class="flex items-center flex-wrap mt-3 gap-2">
                                        <span class="text-xs px-2 py-1 bg-blue-900/30 text-blue-300 rounded">
                                            <i class="far fa-clock mr-1"></i>
                                            ${alarmDate.toLocaleDateString('th-TH', {day: 'numeric', month: 'short'})} 
                                            ${alarmDate.toLocaleTimeString('th-TH', {hour: '2-digit', minute:'2-digit'})}
                                        </span>
                                        <span class="text-xs px-2 py-1 ${timeClass} rounded">
                                            ${timeStatus}
                                        </span>
                                        ${alarm.synced ? '<span class="text-xs px-2 py-1 bg-green-900/30 text-green-300 rounded"><i class="fas fa-cloud mr-1"></i>บันทึก</span>' : ''}
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="ml-4 flex flex-col space-y-2">
                            ${!isTriggered ? `
                                <button onclick="testAlarm('${alarm.id}')" class="text-xs px-3 py-1 bg-yellow-600 hover:bg-yellow-700 rounded text-white transition-colors whitespace-nowrap">
                                    <i class="fas fa-bell mr-1"></i>ทดสอบ
                                </button>
                            ` : ''}
                            ${!isExternal ? `
                                <button onclick="deleteAlarm('${alarm.id}')" class="text-xs px-3 py-1 bg-red-600 hover:bg-red-700 rounded text-white transition-colors whitespace-nowrap">
                                    <i class="fas fa-trash mr-1"></i>ลบ
                                </button>
                            ` : ''}
                        </div>
                    </div>
                `;
                container.appendChild(alarmElement);
            });
        }
        
        function filterAlarmsByType(filterType) {
            const now = new Date();
            const today = new Date();
            today.setHours(0, 0, 0, 0);
            
            switch(filterType) {
                case 'personal':
                    return alarms.filter(alarm => alarm.type === 'personal');
                case 'public':
                    return alarms.filter(alarm => alarm.type === 'public');
                case 'today':
                    return alarms.filter(alarm => {
                        const alarmDate = new Date(alarm.datetime);
                        alarmDate.setHours(0, 0, 0, 0);
                        return alarmDate.getTime() === today.getTime();
                    });
                default:
                    return alarms;
            }
        }
        
        function filterAlarms(filterType) {
            currentFilter = filterType;
            
            ['all', 'personal', 'public', 'today'].forEach(type => {
                const btn = document.getElementById('filter' + type.charAt(0).toUpperCase() + type.slice(1));
                if (btn) {
                    if (type === filterType) {
                        btn.classList.add('filter-active');
                    } else {
                        btn.classList.remove('filter-active');
                    }
                }
            });
            
            updateAlarmsList();
        }
        
        async function deleteAlarm(alarmId) {
            const alarm = alarms.find(a => a.id === alarmId);
            if (!alarm) return;
            
            if (alarm.userId !== userId) {
                showAlert('error', 'คุณไม่มีสิทธิ์ลบแจ้งเตือนนี้ (เป็นของคนอื่น)');
                return;
            }
            
            const result = await Swal.fire({
                title: 'ยืนยันการลบ',
                html: `ลบการแจ้งเตือน<br><strong>"${alarm.title}"</strong><br>หรือไม่?`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'ลบ',
                cancelButtonText: 'ยกเลิก',
                background: '#1f2937',
                color: '#e5e7eb'
            });
            
            if (!result.isConfirmed) return;
            
            alarms = alarms.filter(a => a.id !== alarmId);
            localStorage.setItem('alarms', JSON.stringify(alarms));
            
            updateAlarmsList();
            updateCounts();
            
            showAlert('success', 'ลบการแจ้งเตือนสำเร็จ');
        }
        
        function testAlarm(alarmId) {
            const alarm = alarms.find(a => a.id === alarmId);
            if (!alarm) return;
            
            const typeText = alarm.type === 'personal' ? ' (ส่วนตัว)' : ' (แจ้งทุกคน)';
            showNotification(`ทดสอบ: ${alarm.title}`, alarm.description || 'นี่คือการทดสอบการแจ้งเตือน', 'test');
            
            addNotification({
                id: 'test_' + Date.now(),
                type: 'test',
                title: `ทดสอบ: ${alarm.title + typeText}`,
                message: alarm.description || 'การทดสอบการแจ้งเตือน',
                time: new Date().toISOString(),
                read: false
            });
            
            showAlert('success', 'ส่งการทดสอบการแจ้งเตือนแล้ว');
        }
        
        function testBackgroundNotification() {
            const testAlarm = {
                id: 'test_bg_' + Date.now(),
                title: 'ทดสอบการแจ้งเตือนเบื้องหลัง',
                description: 'นี่คือการทดสอบการทำงานของแจ้งเตือนแม้ปิดแอปและล็อคหน้าจอ',
                datetime: new Date().toISOString(),
                type: 'personal',
                priority: 'high',
                triggered: false
            };
            
            if (document.visibilityState === 'visible') {
                showPushNotification(testAlarm.title, testAlarm.description, 'test');
            }
            
            addNotification({
                id: 'test_bg_' + Date.now(),
                type: 'test',
                title: testAlarm.title,
                message: testAlarm.description,
                time: new Date().toISOString(),
                read: false,
                urgent: true
            });
            
            showAlert('success', 'ส่งการทดสอบแล้ว!');
        }
        
        // ============================================
        // ฟังก์ชันจัดการ Notifications
        // ============================================
        function addNotification(notification) {
            const exists = notifications.find(n => n.id === notification.id);
            if (exists) return;
            
            notifications.unshift(notification);
            
            if (notifications.length > 200) {
                notifications = notifications.slice(0, 200);
            }
            
            localStorage.setItem('notifications', JSON.stringify(notifications));
            
            updateBadges();
            updateCounts();
            
            if (document.getElementById('notificationsPanel').classList.contains('active')) {
                updateNotificationsList();
            }
        }
        
        function updateNotificationsList() {
            const container = document.getElementById('notificationsList');
            
            if (notifications.length === 0) {
                container.innerHTML = `
                    <div class="text-center py-10">
                        <i class="fas fa-bell-slash text-4xl text-gray-500 mb-3"></i>
                        <p class="text-gray-400">ไม่มีการแจ้งเตือน</p>
                        <p class="text-sm text-gray-500 mt-2">เมื่อมีแจ้งเตือนใหม่จะปรากฏที่นี่</p>
                    </div>
                `;
                return;
            }
            
            container.innerHTML = '';
            notifications.forEach(notification => {
                const time = new Date(notification.time);
                const timeAgo = getTimeAgo(time);
                const isRead = notification.read;
                
                const notificationElement = document.createElement('div');
                notificationElement.className = `bg-gray-800 rounded-lg p-4 mb-3 hover:bg-gray-750 transition-colors cursor-pointer ${!isRead ? 'border-l-4 border-blue-500' : ''}`;
                notificationElement.onclick = () => markNotificationAsRead(notification.id);
                notificationElement.innerHTML = `
                    <div class="flex items-start">
                        <div class="w-10 h-10 rounded-full ${notification.type === 'alarm' ? (notification.alarmType === 'public' ? 'bg-green-600' : 'bg-blue-600') : notification.type === 'broadcast' ? 'bg-purple-600' : 'bg-yellow-600'} flex items-center justify-center mr-3 flex-shrink-0">
                            <i class="fas fa-${notification.type === 'alarm' ? 'clock' : notification.type === 'broadcast' ? 'broadcast-tower' : 'bell'} text-white"></i>
                        </div>
                        <div class="flex-1 min-w-0">
                            <div class="flex justify-between items-start">
                                <h3 class="font-bold ${isRead ? 'text-gray-400' : 'text-white'}">${notification.title}</h3>
                                ${notification.urgent ? '<span class="text-xs px-2 py-1 bg-red-900/30 text-red-300 rounded">ด่วน</span>' : ''}
                            </div>
                            <p class="text-gray-300 text-sm mt-1 line-clamp-2">${notification.message}</p>
                            <div class="flex justify-between items-center mt-2">
                                <p class="text-gray-500 text-xs">
                                    <i class="far fa-clock mr-1"></i>
                                    ${timeAgo}
                                </p>
                                ${!isRead ? '<span class="w-2 h-2 rounded-full bg-blue-500"></span>' : ''}
                            </div>
                        </div>
                    </div>
                `;
                container.appendChild(notificationElement);
            });
        }
        
        function markNotificationAsRead(notificationId) {
            const notification = notifications.find(n => n.id === notificationId);
            if (notification && !notification.read) {
                notification.read = true;
                localStorage.setItem('notifications', JSON.stringify(notifications));
                updateBadges();
                updateCounts();
                updateNotificationsList();
            }
        }
        
        function markAllAsRead() {
            notifications.forEach(notification => {
                notification.read = true;
            });
            localStorage.setItem('notifications', JSON.stringify(notifications));
            updateBadges();
            updateCounts();
            updateNotificationsList();
            
            showAlert('success', 'ทำเครื่องหมายอ่านทั้งหมดแล้ว');
        }
        
        function openNotificationPanel() {
            updateNotificationsList();
            document.getElementById('notificationsPanel').classList.add('active');
        }
        
        function closeNotificationPanel() {
            document.getElementById('notificationsPanel').classList.remove('active');
        }
        
        function clearAllNotifications() {
            Swal.fire({
                title: 'ยืนยันการล้าง',
                text: 'ล้างการแจ้งเตือนทั้งหมดหรือไม่?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'ล้างทั้งหมด',
                cancelButtonText: 'ยกเลิก',
                background: '#1f2937',
                color: '#e5e7eb'
            }).then((result) => {
                if (result.isConfirmed) {
                    notifications = [];
                    localStorage.setItem('notifications', JSON.stringify(notifications));
                    updateBadges();
                    updateCounts();
                    updateNotificationsList();
                    
                    showAlert('success', 'ล้างการแจ้งเตือนทั้งหมดแล้ว');
                }
            });
        }
        
        // ============================================
        // ฟังก์ชันอัพเดต UI
        // ============================================
        function updateCounts() {
            document.getElementById('alarmCount').textContent = alarms.length;
            
            const publicCount = alarms.filter(alarm => alarm.type === 'public').length;
            document.getElementById('syncedCount').textContent = publicCount;
            
            document.getElementById('broadcastCount').textContent = broadcasts.length;
            
            const unreadNotifications = notifications.filter(n => !n.read).length;
            document.getElementById('notificationCount').textContent = unreadNotifications;
        }
        
        function updateBadges() {
            const unreadCount = notifications.filter(n => !n.read).length;
            
            const badges = ['headerBadge', 'actionBadge', 'bottomBadge'];
            badges.forEach(badgeId => {
                const badge = document.getElementById(badgeId);
                if (unreadCount > 0) {
                    badge.textContent = unreadCount > 99 ? '99+' : unreadCount;
                    badge.classList.remove('hidden');
                } else {
                    badge.classList.add('hidden');
                }
            });
        }
        
        function getTimeAgo(date) {
            const now = new Date();
            const diff = now - date;
            
            const minutes = Math.floor(diff / 60000);
            const hours = Math.floor(minutes / 60);
            const days = Math.floor(hours / 24);
            
            if (minutes < 1) return 'เมื่อสักครู่';
            if (minutes < 60) return `${minutes} นาทีที่แล้ว`;
            if (hours < 24) return `${hours} ชั่วโมงที่แล้ว`;
            if (days < 7) return `${days} วันที่แล้ว`;
            
            return date.toLocaleDateString('th-TH');
        }
        
        function showConnectionStatus(connected, message, details) {
            const statusDot = document.getElementById('statusDot');
            const statusText = document.getElementById('statusText');
            const connectionMessage = document.getElementById('connectionMessage');
            const connectionDetails = document.getElementById('connectionDetails');
            
            isOnline = connected;
            
            if (connected) {
                statusDot.className = 'w-2 h-2 rounded-full bg-green-500 mr-2';
                statusText.textContent = 'ออนไลน์';
                statusText.className = 'text-xs online';
                connectionMessage.textContent = message;
                connectionDetails.textContent = details;
                
            } else {
                statusDot.className = 'w-2 h-2 rounded-full bg-red-500 mr-2';
                statusText.textContent = 'ออฟไลน์';
                statusText.className = 'text-xs offline';
                connectionMessage.textContent = message;
                connectionDetails.textContent = details;
            }
        }
        
        function setupNetworkListener() {
            window.addEventListener('online', () => {
                showConnectionStatus(true, 'กลับมาเชื่อมต่อแล้ว', 'ระบบพร้อมใช้งาน');
                checkForPublicAlarms();
            });
            
            window.addEventListener('offline', () => {
                showConnectionStatus(false, 'สูญเสียการเชื่อมต่อ', 'ทำงานเฉพาะในเครื่อง');
            });
            
            if (navigator.onLine) {
                showConnectionStatus(true, 'กำลังเชื่อมต่อ...', 'ตรวจสอบการเชื่อมต่อ');
            } else {
                showConnectionStatus(false, 'ออฟไลน์', 'ทำงานเฉพาะในเครื่อง');
            }
        }
        
        // ============================================
        // ฟังก์ชันช่วยเหลือ
        // ============================================
        function showAlert(icon, title, text = '') {
            Swal.fire({
                icon: icon,
                title: title,
                text: text,
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 3000,
                background: '#1f2937',
                color: '#e5e7eb'
            });
        }
        
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }
        
        function scrollToForm() {
            document.getElementById('alarmFormSection').scrollIntoView({ 
                behavior: 'smooth',
                block: 'start'
            });
        }
        
        function scrollToBroadcast() {
            document.getElementById('broadcastSection').scrollIntoView({ 
                behavior: 'smooth',
                block: 'start'
            });
        }
        
        function refreshAllData() {
            const refreshBtn = document.getElementById('refreshBtn');
            refreshBtn.classList.add('animate-spin');
            
            loadAppData();
            testConnection();
            
            setTimeout(() => {
                refreshBtn.classList.remove('animate-spin');
                showAlert('success', 'รีเฟรชข้อมูลเรียบร้อยแล้ว');
            }, 1000);
        }
        
        function checkNotificationPermission() {
            if (!('Notification' in window)) {
                console.log('❌ เบราว์เซอร์นี้ไม่รองรับการแจ้งเตือน');
                return;
            }
            
            if (Notification.permission === 'default') {
                document.addEventListener('click', function requestPermissionOnce() {
                    if (Notification.permission === 'default') {
                        Notification.requestPermission().then(permission => {
                            if (permission === 'granted') {
                                console.log('✅ ได้รับอนุญาตการแจ้งเตือนแล้ว');
                            }
                        });
                    }
                    document.removeEventListener('click', requestPermissionOnce);
                });
            }
        }
        
        function showWelcomeGuide() {
            Swal.fire({
                title: 'ยินดีต้อนรับสู่ระบบแจ้งเตือนรวมศูนย์',
                html: `
                    <div class="text-left space-y-4">
                        <div class="bg-blue-900/30 rounded-lg p-4">
                            <h4 class="font-bold text-blue-300 mb-2"><i class="fas fa-bell mr-2"></i>วิธีใช้งาน</h4>
                            <ol class="text-sm space-y-2">
                                <li>1. <strong>ตั้งเวลาแจ้งเตือน</strong> - ใช้แบบฟอร์มด้านบน</li>
                                <li>2. <strong>ส่งประกาศ</strong> - แจ้งเตือนไปทุกเครื่อง</li>
                                <li>3. <strong>อนุญาตการแจ้งเตือน</strong> - สำหรับการทำงานที่สมบูรณ์</li>
                            </ol>
                        </div>
                    </div>
                `,
                icon: 'info',
                confirmButtonText: 'เริ่มใช้งาน',
                background: '#1f2937',
                color: '#e5e7eb',
                width: 500
            });
        }
        
        function handleAlarmTriggered(alarm) {
            console.log('🔔 Alarm triggered from Service Worker:', alarm.title);
            
            const index = alarms.findIndex(a => a.id === alarm.id);
            if (index !== -1) {
                alarms[index].triggered = true;
                alarms[index].triggeredAt = new Date().toISOString();
                localStorage.setItem('alarms', JSON.stringify(alarms));
            }
            
            const typeText = alarm.type === 'personal' ? ' (ส่วนตัว)' : ' (แจ้งทุกคน)';
            addNotification({
                id: 'sw_alarm_' + alarm.id,
                type: 'alarm',
                title: alarm.title + typeText,
                message: alarm.description || 'เวลาแจ้งเตือนถึงแล้ว!',
                time: new Date().toISOString(),
                alarmId: alarm.id,
                read: false,
                urgent: true,
                alarmType: alarm.type
            });
            
            if (document.visibilityState === 'visible') {
                updateAlarmsList();
                updateCounts();
                updateBadges();
            }
        }
        
        function handleNotificationClicked(data) {
            console.log('🔘 Notification clicked:', data);
            
            if (data.type === 'alarm') {
                openNotificationPanel();
            }
        }
        
        // ============================================
        // เริ่มต้นแอปพลิเคชัน
        // ============================================
        document.addEventListener('DOMContentLoaded', initApp);
        
        document.addEventListener('visibilitychange', function() {
            if (document.visibilityState === 'visible') {
                updateBadges();
            }
        });
        
        console.log('🎉 App initialized successfully');
    </script>
</body>
</html>
